# Параметры подготовки данных
prepare:
  test_size: 0.2
  random_state: 42

# Параметры для обучения
train:
  model_type: "random_forest"
  n_estimators: 100
  max_depth: 5
  random_state: 42
  
  # Альтернативно: logistic regression
  logistic_regression:
    C: 1.0
    max_iter: 200
